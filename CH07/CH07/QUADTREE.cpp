#include<iostream>
#include<vector>
#include<string>
using namespace std;

string quadTree(string &str) {
	if (str.compare("w") == 0) return "w";
	if (str.compare("b") == 0) return "b";

	if (str.at(0) == 'x') {
		//??
	}
	
}

int main(void) {

	int num;
	cin >> num;
	for (int a = 0; a < num; a++) {

		string input;
		cin >> input;
		



	}
	return 0;
}